# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/
*.local

# Environment — never commit real secrets.
# .env.* already covers .env.local and every other variant; both are listed
# explicitly so the intent is obvious to anyone reading this file.
.env
.env.local
.env.*
!.env.example

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# Test / tooling caches
coverage/
.eslintcache
.vite/
.turbo/
*.tsbuildinfo

# Supabase local state
supabase/.branches
supabase/.temp
supabase/.env

# Scratch
/tmp-scratch/
playwright-report/
test-results/
screenshots/

# Local throwaway helpers used while developing (not part of the project).
# The audits that matter live in scripts/ and are committed.
/.*.mjs
/.devserver.sh
